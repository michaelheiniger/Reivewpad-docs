api-version: reviewpad.com/v3.x

workflows:
  - name: check-compliance
    if:
      - $hasLinearHistory() == false
    then:
      - $error("Pull request is out of sync with the base branch")
      - $fail()
