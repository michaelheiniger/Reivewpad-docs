api-version: reviewpad.com/v3.x

workflows:
  - name: workload
    if:
      - $totalCreatedPullRequests($author(), "open") > 2
    then:
      - $commentOnce("You have too many open pull requests.")
