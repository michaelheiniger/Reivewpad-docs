---
slug: "/guides/built-ins-totalcreatedpullrequests"
---

import CodeBlock from "@theme/CodeBlock";
import Example from "!!raw-loader!./example.yml";

:::danger DEPRECATED

This built-in is deprecated and will be removed in the next major version.

Please use the [pullRequestCountBy](./built-ins#pullrequestcountby) built-in instead.

:::

**Description**:

Retrieves the total number of pull requests created by the given GitHub user login.

**Parameters**:

| Parameter    | Type   | Description            |
| :----------- | :----- | :--------------------- |
| `user_login` | string | The GitHub user login. |

**Return value**:

| Type  | Description                                                         |
| :---- | :------------------------------------------------------------------ |
| `int` | The total number of pull requests created by the GitHub user login. |

**Examples**:

```yml
$totalCreatedPullRequests($author())
```

A `reviewpad.yml` example:

<CodeBlock language="yml" title="reviewpad.yml">{Example}</CodeBlock>
