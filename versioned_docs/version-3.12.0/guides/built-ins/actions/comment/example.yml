api-version: reviewpad.com/v3.x

workflows:
  - name: update-to-date
    if:
      - $hasLinearHistory() == false
    then:
      - $comment("The pull request is out of sync with the base branch.")
      - $fail()
